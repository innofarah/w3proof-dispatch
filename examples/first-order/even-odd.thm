%% Proof that every natural number is even or odd
%%
%% This is a simple example where induction is needed at a nested rather
%% than immediate recursive call.
Kind      nat                 type.

Import "even-odd-two".


Theorem two-even : even (s (s z)).
unfold. search. 